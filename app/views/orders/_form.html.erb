<%= form_for(order) do |f| %>
    <div class="form-group">
      <% if order.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

            <ul>
              <% order.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
            </ul>
          </div>
      <% end %>

      <div class="field">
        <%= f.label :lading_no %>
        <%= f.text_field :lading_no, class: "form-control" %>
      </div>

      <div class="field">
        <%= f.label :customer %>
        <%= f.text_field :customer, class: "form-control" %>
      </div>

      <div class="field">
        <%= f.label :address %>
        <%= f.text_field :address, class: "form-control" %>
      </div>

      <div class="field">
        <%= f.label :phone %>
        <%= f.text_field :phone, class: "form-control" %>
      </div>

      <div class="field">
        <%= f.label :sale_name %>
        <%= f.text_field :sale_name, class: "form-control" %>
      </div>

      <div class="field">
        <%= f.label :item_type %>
        <%= f.text_field :item_type, class: "form-control" %>
      </div>

      <div class="field">
        <%= f.label :install_date %>
        <%= f.date_field :install_date, class: "form-control" %>
      </div>

      <div class="field">
        <%= f.label :team_name %>
        <%= f.text_field :team_name, class: "form-control" %>
      </div>

      <div class="field">
        <%= f.label :note %>
        <%= f.text_field :note, class: "form-control" %>
      </div>

      <div class="btn btn-default">
        <%= f.submit %>
      </div>

    </div>
<% end %>
